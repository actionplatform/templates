pipeline {
    agent { docker { image 'composer:2' } }

    stages {
        stage('Code Quality') {
            steps {
                sh 'composer install --no-interaction --prefer-dist'
                sh 'composer lint'
                sh 'composer analyse'
                sh 'composer test'
            }
        }
    }
}
