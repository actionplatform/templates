pipeline {
    agent { docker { image 'node:20' } }

    stages {
        stage('Code Quality') {
            steps {
                sh 'npm ci'
                sh 'npm run lint'
                sh 'npm run format'
                sh 'npm test'
                sh 'npm run build'
            }
        }
    }
}
