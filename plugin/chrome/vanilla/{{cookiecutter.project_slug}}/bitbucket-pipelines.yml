image: node:22

pipelines:
  pull-requests:
    '**':
      - step:
          name: Code Quality
          script:
            - npm ci
            - npm run lint
            - npm run format
            - node scripts/check_manifest.js
            - npm test
