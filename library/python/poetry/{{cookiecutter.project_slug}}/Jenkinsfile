pipeline {
    agent { docker { image 'python:3.12' } }

    stages {
        stage('Code Quality') {
            steps {
                sh 'pip install poetry'
                sh 'poetry install --with dev,code-quality'
                sh 'poetry run ruff check .'
                sh 'poetry run ruff format --check .'
            }
        }
    }
}
