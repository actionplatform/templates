pipeline {
    agent { docker { image 'golang:1.23' } }

    stages {
        stage('Code Quality') {
            steps {
                sh 'go mod download'
                sh 'go vet ./...'
                sh 'test -z "$(gofmt -l .)"'
            }
        }
    }
}
