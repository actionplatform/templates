pipeline {
    agent { docker { image 'rust:1.80' } }

    stages {
        stage('Code Quality') {
            steps {
                sh 'cargo fmt --check'
                sh 'cargo clippy -- -D warnings'
            }
        }
    }
}
